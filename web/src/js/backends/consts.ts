
export enum ReverseProxyProtocols {
    HTTP = "http",
    HTTPS = "https",
    HTTP3 = "http3",
    TLS = "tls",
    DTLS = "dtls",
    TCP = "tcp",
    UDP = "udp",
    DNS = "dns",
    QUIC = "quic",
}

export enum SyntaxHighlight {
    CSS = "css",
    JAVASCRIPT = "javascript",
    JSON = "json",
    XML = "xml",
    YAML = "yaml",
    PYTHON = "python",
    NONE = "none",
    ERROR = "error",
}
